// use cpal::{default_host, traits::HostTrait, Device};
// // use futures_util::Sink;

// pub struct DefaultSink {
//     device: Device,
// }

// impl DefaultSink {
//     pub fn new() -> Option<Self> {
//         let host = default_host();
//         host.default_output_device().map(|d| Self { device: d })
//     }
// }
